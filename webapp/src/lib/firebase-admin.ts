export {}; // stub until we need it
// Firebase Admin SDK se puede inicializar aquí cuando lo necesitemos 
// en una API Route protegida, aunque podemos verificar con `auth.currentUser` directamente.
